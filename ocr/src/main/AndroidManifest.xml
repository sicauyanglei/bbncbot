<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight"
        tools:targetApi="34">

        <!-- 启动页：仅展示"OCR 模块已安装"，无实际交互 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            OCR 识别 Service
            - exported=true：允许主 APK（:app）跨进程绑定
            - 不用 signature 级权限保护（避免签名不匹配导致 bindService 被拒）
            - 改在 OcrService.onBind() 中检查调用方包名是否为 com.bbncbot，非白名单包返回 null
            - 安全性：第三方需知道 action + package 才能 bind，且 onBind 会拒绝非白名单调用方
        -->
        <service
            android:name=".OcrService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.bbncbot.ocr.action.RECOGNIZE" />
            </intent-filter>
        </service>

    </application>

</manifest>
