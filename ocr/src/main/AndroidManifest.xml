<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight"
        tools:targetApi="34">

        <!-- 启动页：仅展示"OCR 模块已安装"，无实际交互 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            OCR ContentProvider
            - authorities: com.bbncbot.ocr.provider
            - exported=true：允许主 APK 跨进程调用
            - 不用 android:permission：signature 权限会导致系统在查找 provider 时
              预检查调用方权限，若权限未授予（先定义后授予陷阱/签名差异），
              provider 被过滤 → Unknown authority
            - 安全由 OcrContentProvider.call() 内部包名白名单保证
              (allowedCallers = setOf("com.bbncbot"))，非白名单直接返回 null
        -->
        <provider
            android:name=".OcrContentProvider"
            android:authorities="com.bbncbot.ocr.provider"
            android:exported="true" />

    </application>

</manifest>
